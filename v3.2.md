- fix lỗi Pipeline thất bại: local variable 'input_dir' referenced before assignment
- Viết thêm các hàm pytest và test. 
- Input lưu về  thư mục resources/input/{unique_str}/{address}/. Hiện tại đang lưu vào autohdr_backend/output/temp_uploads
- Không tìm thấy file log để có thể trace lại log trong sau này file này sẽ lưu vào resources/logs/{unique_str}/{address}/ và mỗi lần run pipeline sẽ tạo ra 1 file log riêng biệt để hiển thị cho người dùng xem. vậy nên ở phía frontend cần có nút clear hoặc mỗi khi process lại thì clear ở phía UI, để hiển thị log mới. và để tránh log dài quá với layout thì cho thanh scroll xuống dưới cùng khi có log mới và có thể cuộn lên để xem lại log cũ. Thêm phần chịu lỗi tốt hơn để khi pipeline lỗi thì vẫn có thể tiếp tục xử lý các bước sau và có thể run lại pipeline từ đầu bằng UI