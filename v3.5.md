Khi pipeline đang chạy. User tắt trang thì khi vào lại cần check job id đã hoàn thành chưa, nếu đã hoàn thành và đã tải rồi thì không hỏi lại user. còn nếu hoàn thành nhưng chưa tải thì hỏi lại user và tự động download các file chưa tải về nếu user đồng ý. nếu job id vẫn đang xử lý thì không cho phép tạo job mới, và tiếp tục hiển thị log của job đó cho người dùng. 

Hiện tại có vẻ như đang sử dụng api để gọi đến api/status để check job id. tuy nhiên sẽ có vấn đề nếu job này run kéo dài 10p và có nhiều user sử dụng cùng lúc thì hiệu suất, tốn tài nguyên, cần phải tối ưu lại. 
