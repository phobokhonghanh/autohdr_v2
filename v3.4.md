- có vẻ như chưa migration qua hết. hiện tại bạn đang sử dụng file session autohdr_backend/sessions.json chứ khong phải sử dụng của autohdr_backend/resources/system/sessions.json 
- log hiện tại đang để date là thư mục, còn tôi muốn nó là file và ghi vào đó, mỗi lần pipeline chạy thì log sẽ được ghi vào thư mục date với ngày hiện tại và ngăn cách các lần khác bằng cách sau mỗi pipeline (lần chạy) sẽ thêm 1 khoảng cách sử dụng dấu =
- hiện tại hệ thống đang tự tạo ra 2 thư mục  khong sử dụng, hãy xóa phần tạo ra 2 thư mục này. autohdr_backend/resources/logs và autohdr_backend/resources/input 
- Ở frontend thì sau khi báo pipeline complete thì hãy clear element input cookie, ảnh vì nó vẫn còn giữ tên file trước đó đã input lên. còn input email vẫn giữ lại 